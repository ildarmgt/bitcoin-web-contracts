module.exports = {
  // relative pathing, allows use from file system, must use hash router
  publicPath: '',

  // npm run build puts build into /docs/ fodler for github pages
  outputDir: 'docs'
};
